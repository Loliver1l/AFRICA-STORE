@extends('layouts.app') @section('content')
<h1>Store</h1><form action="{{route('products.search')}}"><input name="q" placeholder="Search products..." value="{{request('q')}}"><button class="btn">Search</button></form><div class="grid">@forelse($products as $product)<div class="card"><h3>{{ $product->name }}</h3><p>{{ Str::limit($product->description,100) }}</p><div class="price">${{number_format($product->price_usd,2)}}</div><p>Status: {{str_replace('_',' ',ucfirst($product->status))}}</p><a class="btn" href="{{route('products.show',$product)}}">View</a></div>@empty<p>No products found.</p>@endforelse</div>{{ $products->links() }}@endsection