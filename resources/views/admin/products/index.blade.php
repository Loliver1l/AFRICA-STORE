@extends('layouts.app') @section('content')<h1>Products</h1><a class="btn" href="{{route('admin.products.create')}}">Add product</a><br><br>@foreach($products as $product)<div class="card"><h3>{{$product->name}}</h3><p>${{number_format($product->price_usd,2)}} | {{$product->status}} | Stock {{$product->stock}}</p><a class="btn" href="{{route('admin.products.edit',$product)}}">Edit</a><form style="display:inline" method="POST" action="{{route('admin.products.destroy',$product)}}">@csrf @method('DELETE')<button class="btn">Delete</button></form></div><br>@endforeach{{$products->links()}}@endsection